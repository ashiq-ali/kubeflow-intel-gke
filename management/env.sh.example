#!/usr/bin/env bash
# Management cluster configuration
# Copy to env.sh and fill in your values — env.sh is gitignored

export MGMT_PROJECT=my-gcp-project
export MGMT_NAME=kubeflow-mgmt
export LOCATION=us-central1        # Must be an Anthos Config Controller supported region
